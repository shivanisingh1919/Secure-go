<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myproject">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".tabLogin"
            android:label="@string/title_activity_tab_login"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".history" />
        <activity android:name=".decryptionend" />
        <activity android:name=".decryptionkey" />
        <activity android:name=".decryption" />
        <activity android:name=".encend" />
        <activity android:name=".keycreation" />
        <activity android:name=".carousel" />
        <activity android:name=".browseimage" />
        <activity android:name=".operation" />
        <activity android:name=".login" />
        <activity android:name=".register" />
        <activity android:name=".home" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>